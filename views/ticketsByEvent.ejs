<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Tickets for <%= eventName %> - TicketHaven</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
    <style>
      body {
        background-color: #0e101c;
        color: #f0f0f0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }

      .card-custom {
        background-color: #1c1f2e !important; /* rich dark indigo */
        border: 1px solid #2e3244;
        border-radius: 16px;
        color: #e0e0e0;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
      }

      .card-custom:hover {
        transform: scale(1.02);
        box-shadow: 0 0 20px rgba(255, 165, 0, 0.2);
      }

      .badge-section {
        background-color: #9b59b6; /* purple */
        font-size: 0.85rem;
      }

      .badge-time {
        background-color: #3498db; /* blue */
        font-size: 0.85rem;
      }

      .badge-price {
        background-color: rgb(220, 53, 69);; /* soft red */
        font-size: 0.85rem;
      }

      .btn-outline-success {
        border-color: #2ecc71;
        color: #2ecc71;
      }

      .btn-outline-success:hover {
        background-color: #2ecc71;
        color: #fff;
      }

      h1 {
        color: #f9d923;
      }

      .ticket-title {
        font-size: 1.25rem;
        color: #f1c40f;
        font-weight: bold;
      }

      .ticket-info {
        color: #bdc3c7;
      }
    </style>
  </head>
  <body>
    <div class="container py-5">
      <h1 class="text-center mb-5">🎟️ Tickets for "<%= eventName %>"</h1>

      <div class="row g-4">
        <% tickets.forEach(ticket => { %>
        <div class="col-md-6 col-lg-4">
          <div class="card card-custom p-4 h-100 d-flex flex-column justify-content-between">
            <div>
              <div class="ticket-title mb-2"><%= ticket.eventName %></div>
              <div class="ticket-info mb-3">Section: <strong><%= ticket.sectionName %></strong></div>
              <div class="mb-2">
                <span class="badge badge-time me-2">📅 <%= ticket.eventDate %> ⏰ <%= ticket.eventTime %></span>
              </div>
              <div>
                <span class="badge badge-price">$<%= ticket.price %></span>
              </div>
            </div>
            <div class="d-grid mt-4">
              <button class="btn btn-outline-success btn-sm">Book Ticket</button>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <%- include("partials/footer") %>
  </body>
</html>
